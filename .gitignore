node_modules/
*.log
.DS_Store
Thumbs.db
scripts/build/
scripts/dist/
scripts/__pycache__/

# Bản đóng gói phái sinh để gửi người dùng cuối — build lại từ repo này bất cứ lúc nào, không cần
# trùng lưu trong git (tránh nhân đôi file .exe/.ccx nặng).
Mic Check Plugin - Share/
Mic Check Plugin - Share.zip

# LƯU Ý: bản .ccx chính thức ở gốc repo đặt tên kèm hậu tố version (vd MicCheck_v1.5.0.ccx) — ĐÃ lưu
# trong git bình thường, không bị ignore. Bản test gửi qua chat lúc dev luôn build ra $env:TEMP
# (ngoài repo) nên không bao giờ lọt vào đây.

# Media test assets (quá lớn cho git — vd clip background.mp4 193MB vượt giới hạn GitHub 100MB)
Premiere test/file import/*.mp4
Premiere test/file import/*.mov
Premiere test/file import/*.mxf
Premiere test/file import/*.avi
Premiere test/Adobe Premiere Pro Auto-Save/
